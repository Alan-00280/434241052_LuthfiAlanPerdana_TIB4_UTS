/// Sistem spacing global (8pt grid) untuk seluruh aplikasi.
/// DILARANG hardcode EdgeInsets di widget — gunakan AppSpacing.*.
class AppSpacing {
  AppSpacing._();

  /// 4.0 — jarak sangat kecil
  static const double xs = 4.0;

  /// 8.0 — jarak kecil
  static const double sm = 8.0;

  /// 16.0 — jarak standar / default
  static const double md = 16.0;

  /// 24.0 — jarak besar
  static const double lg = 24.0;

  /// 32.0 — jarak sangat besar
  static const double xl = 32.0;
}
